# tiny-c

C言語の超サブセットで書かれたソースコードをコンパイルする、教育用Cコンパイラ。
ターゲットは x86 Linux。最も単純で小さなソースコードであることを重視する。

## アーキテクチャ

- 実装言語: C + flex/bison
- パーサ: bison で定義
- レキサ: flex で定義
- 出力: x86 アセンブリ (.s ファイル) を生成し、gcc/as でリンクして実行バイナリを得る
- ビルド: Makefile

## tiny-c 文法一覧

### 型
| 型         | 例                        |
|-----------|--------------------------|
| `int`     | `int x;`                 |
| `char`    | `char c;`                |
| `char*`   | `char *s;`               |
| `int*`    | `int *p;`                |
| `void`    | 関数の戻り値型のみ         |

- ポインタは1段階のみ (`int *p` は可、`int **pp` は不可)
- 配列は1次元のみ (`int a[10];`)

### リテラル
| 種類       | 例                        |
|-----------|--------------------------|
| 整数       | `0`, `42`, `-1`          |
| 文字       | `'a'`, `'\n'`            |
| 文字列     | `"hello"`                |

### 変数
- ローカル変数宣言: 初期化子必須 (`int x = 0;` `char *s = "hello";`)
- グローバル変数宣言: `int g;` (初期化子なし。ゼロ初期化)
- 配列宣言: `int a[10];` (初期化子なし)

### 演算子（優先順位の高い順）

| 優先度 | 演算子               | 説明                 |
|-------|---------------------|---------------------|
| 1     | `()` `[]`           | 関数呼び出し、添字    |
| 2     | `*` (単項) `&` `-` (単項) `!` | 間接参照、アドレス取得、符号反転、論理否定 |
| 3     | `*` `/` `%`         | 乗除・剰余           |
| 4     | `+` `-`             | 加減                 |
| 5     | `<` `<=` `>` `>=`   | 比較                 |
| 6     | `==` `!=`           | 等値比較             |
| 7     | `=`                 | 代入                 |

### 文
```
文 :
    式 ;
    return 式 ;
    return ;
    { 文* }
    if ( 式 ) 文
    if ( 式 ) 文 else 文
    while ( 式 ) 文
    型 変数名 = 式 ;
    型 変数名 [ 整数 ] ;
```

### 関数
```
関数定義 : 型 関数名 ( 仮引数リスト? ) { 文* }
仮引数   : 型 変数名
関数呼出 : 関数名 ( 実引数リスト? )
```

- 引数は最大6個（x86 Linux ABI の レジスタ渡し範囲）
- `printf` 等の外部関数は宣言なしで呼び出し可能（リンク時に解決）

### プログラム構造
```
プログラム : (グローバル変数宣言 | 関数定義)*
```

- `main` 関数がエントリポイント

### 非対応（意図的に除外）
- `for`, `switch`, `?:`, `do-while` （`for` は `while` で代替可能）
- `&&`, `||` （短絡評価。`!` と `if` のネストで代替可能）
- 未初期化のローカル変数宣言 (`int x;` のような形式は不可。必ず初期化子を書く)
- 構造体、共用体、enum
- float / double
- プリプロセッサ (#include, #define 等)
- typedef, sizeof, キャスト
- 多次元配列、関数ポインタ、多段ポインタ
- 可変長引数
- コンマ演算子、ビット演算子
- `+=` `-=` `++` `--` 等の複合代入・インクリメント

## 設計方針

- **実装コストが高いC言語の機能は積極的に削る。** ただし削りすぎて「何も書けない言語」にはしない。
- 判断基準: 「この機能がないと、意味のあるサンプルプログラム（再帰・ループ・ポインタ操作）が書けなくなるか？」で取捨選択する。
- 迷ったら削る。後から必要になれば追加すればよい。

## コーディング規約

- ソースコード中のコメントは英語で書く
- ドキュメント・CLAUDE.md 等は日本語
- 教育目的のため、コードの簡潔さ・読みやすさを最優先する

## ドキュメント章立て（`docs/`）

各章で **完全に動く小さなコンパイラ** を提示する構成。章が進むごとに言語機能が増え、コンパイラが育っていく。

各章ごとに `docs/chNN/` フォルダを作り、その章のテーマを複数のサブファイル（`00_overview.md` `01_..._md` ...）に分けて詳説する。flex / bison / コンパイラ本体（AST, codegen, main, Makefile）を **その章時点の完全形** で提示する。

各章のソースコードは `steps/chNN/` 配下に置き、実際にビルド・実行可能な最小構成として保存する。

### 章一覧

| 章 | フォルダ | コンパイルできるようになるもの | 焦点 |
|---|---------|-----|------|
| 1 | `docs/ch01/` | `int main() { return 42; }` | パイプライン全体（lexer→parser→AST→codegen→build）の土台 |
| 2 | `docs/ch02/` | `return 2 + 3 * 4;`（四則演算と優先順位） | 文法が優先順位を表現する仕組み、スタックを使った中間値の管理 |
| 3 | `docs/ch03/` | `int x = 1; int y = 2; return x + y;` | スタックフレーム上のアドレスとして変数を実現、シンボルテーブル |
| 4 | `docs/ch04/` | `if/else` `while` と比較演算子（階乗計算など） | CPU は条件ジャンプしか知らない、ラベル生成 |
| 5 | `docs/ch05/` | 関数定義・引数・再帰・`printf` 呼び出し | System V AMD64 ABI、レジスタ渡し、16バイトスタックアライメント |
| 6 | `docs/ch06/` | `char *s = "hello"; printf("%s\n", s);` ポインタ・配列・グローバル変数 | lvalue/rvalue の二面性、`gen_addr` 関数、`&` と `*` の対称性 |
| 7 | `docs/ch07/` | （言語機能の追加なし。最適化を導入） | AST 最適化（定数畳み込み + 代数的単純化）、ピープホール最適化、コンパイラの拡張性 |

### 各章の構成パターン

```
1. この章で書けるようになるコード（完成形を先に見せる）
2. 何が足りないか（前章との差分を明確に）
3. lexer.l — その章時点の完全形（差分をハイライト）
4. parser.y — その章時点の完全形（差分をハイライト）
5. ast.h / ast.c — その章時点の完全形（差分をハイライト）
6. codegen.c — その章時点の完全形（差分をハイライト）
7. main.c / Makefile — 章をまたいで変わるところだけ
8. ビルドして動かす（実際のコマンドと出力）
9. 生成されるアセンブリを読む
10. 次章への橋渡し（今のコンパイラにはまだ○○ができない）
```

- ch01 は導入として最重要なため、複数のサブファイル（regex / flex / bison / AST / codegen / build）に分けて丁寧に説明する。
- ch02 以降は、ch01 で土台ができている前提で、その章の **新しい概念** に焦点を絞って簡潔に書く。

### 語り口のガイドライン

- 各章の冒頭は「この章が終わると書けるコード」を見せる（完成品ファースト）。
- 技術用語は初出時に文脈の中でさりげなく説明する（独立した用語集にはしない）。
- 「なぜこうなっているか」を重視し、「こういう仕組みです」の網羅的羅列は避ける。
- コードが小さいことを活かす — 「たったこれだけで動く」感を大事にする。

## ビルド・テスト

```bash
make          # ビルド
make test     # テスト実行
```

### テスト方針
- シェルスクリプトでテストケースを実行
- .c ファイルをコンパイル → 実行 → 終了コードや出力を検証
